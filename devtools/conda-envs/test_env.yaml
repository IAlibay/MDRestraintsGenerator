name: test
channels: 
  - conda-forge
dependencies:
    # Base depends
  - python
  - pip
  - MDAnalysis=0.20.1
  - matplotlib
  - scipy
    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

